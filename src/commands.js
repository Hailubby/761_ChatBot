/**
 * Commands record the responses that user has sent to the bot to
 * determine the next message that we need to send.
 */
class Commands {
  constructor(){
    this.position = 0;
    this.traversalHistory = ["strategies", "yes", "potato"];
  }
}
